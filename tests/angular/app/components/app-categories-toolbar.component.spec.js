ngDescribe({
    name: 'Test app-categories-toolbar component',
    modules: 'app',
    element: '<app-categories-toolbar></app-categories-toolbar>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
