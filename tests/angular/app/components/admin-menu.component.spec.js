ngDescribe({
    name: 'Test admin-menu component',
    modules: 'app',
    element: '<admin-menu></admin-menu>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
