ngDescribe({
    name: 'Test image-uploader component',
    modules: 'app',
    element: '<image-uploader></image-uploader>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
