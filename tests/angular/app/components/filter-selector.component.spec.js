ngDescribe({
    name: 'Test filter-selector component',
    modules: 'app',
    element: '<filter-selector></filter-selector>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
