ngDescribe({
    name: 'Test loader component',
    modules: 'app',
    element: '<loader></loader>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
