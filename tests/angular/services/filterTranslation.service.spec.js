ngDescribe({
    name: 'Test FilterTranslationService',
    modules: 'app',
    inject: 'FilterTranslationService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
