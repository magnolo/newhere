ngDescribe({
    name: 'Test CategoryTranslationService',
    modules: 'app',
    inject: 'CategoryTranslationService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
